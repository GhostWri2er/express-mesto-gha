const APPROVED = 200;
// eslint-disable-next-line max-len
const ERROR_CODE = 400;
const NOT_FOUND = 404;
const SERVER_ERROR = 500;

module.exports = {
  ERROR_CODE, NOT_FOUND, SERVER_ERROR, APPROVED,
};
